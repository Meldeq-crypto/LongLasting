# Everlasting Fixes

Stops session-based lag and memory leaks for indefinite playtimes.

## Requirements
- Minecraft 1.21.1–1.21.11
- Fabric Loader >= 0.16.10
- Java 21+
- Fabric API (client)

## Build
```bash
gradlew.bat clean build -x test
```

The runtime JAR is output to `app/build/libs/`.
